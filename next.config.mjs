/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/portfolio-2', 
    images: {
      unoptimized: true, 
    },
  };
  
  export default nextConfig;
  